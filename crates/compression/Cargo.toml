[package]
name = "benshu-compression"
description = "Reusable compression primitives for BenShu outputs, context, evidence, and runtime surfaces"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
