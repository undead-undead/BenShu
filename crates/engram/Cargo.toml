[package]
name = "benshu-engram"
version.workspace = true
edition.workspace = true
authors = ["BenShu Team"]
description = "Engram — High-performance memory engine for BenShu with Engram-KV storage, SIMD-accelerated vector search, and pure Rust BM25"
license = "MIT OR Apache-2.0"
exclude = ["*.db"]

[dependencies]
# Core async runtime
tokio = { version = "1.35", features = ["full"] }
async-trait = "0.1"
async-recursion = "1.1"
benshu-compression = { workspace = true }
benshu-inference = { workspace = true }
benshu-infra = { workspace = true }
benshu-memory-api = { workspace = true }
benshu-memory-core = { workspace = true }
benshu-protocol-core = { workspace = true }
benshu-scheduler = { workspace = true }

# Engram-KV storage engine (pure Rust, mmap, ACID)
redb = "2.4"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1.3"
bytes = "1.5"

# Hashing (content-addressable storage)
sha2 = "0.10"
hex = "0.4"

# Date/Time
chrono = { version = "0.4", features = ["serde"] }

# UUID generation
uuid = { version = "1.6", features = ["v4", "serde"] }

# Error handling
thiserror = "1.0"
anyhow = "1.0"
parking_lot = "0.12"

# Logging
tracing = "0.1"
rayon = "1.8"

# Glob patterns
glob = "0.3"

# File watching for Active Indexing
notify = "6.1"

# CJK segmentation support
jieba-rs = "0.6"
once_cell = "1.18"
unicode-normalization = "0.1"

# HNSW vector index
hnsw_rs = { version = "0.3", optional = true }

# Candle ML framework for embeddings
candle-core = { version = "0.8", optional = true }
candle-nn = { version = "0.8", optional = true }
candle-transformers = { version = "0.8", optional = true }
tokenizers = { version = "0.21", optional = true }

# SIMD acceleration
simsimd = "6.2"

# Futures
futures.workspace = true
lru = "0.16.3"
encoding_rs = "0.8.35"
regex.workspace = true

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["processthreadsapi", "winbase", "winnt", "sysinfoapi"] }

[dev-dependencies]
tempfile = "3.8"
tokio-test = "0.4"
tracing-subscriber = "0.3"

[features]
default = ["fts", "vector"]
fts = []
vector = ["hnsw_rs", "candle-core", "candle-nn", "candle-transformers", "tokenizers"]
simd = []
full = ["fts", "vector", "simd"]
tokenizers = ["dep:tokenizers"]
